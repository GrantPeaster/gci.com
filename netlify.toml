# Georgia Civil — Netlify configuration
# Static site served from the repo root; serverless functions in netlify/functions.

[build]
  publish = "."
  functions = "netlify/functions"

[functions]
  node_bundler = "esbuild"
